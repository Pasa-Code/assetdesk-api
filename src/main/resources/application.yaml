spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/assetdesk
    username: assetdesk
    password: assetdesk
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info