services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: assetdesk
      POSTGRES_USER: assetdesk
      POSTGRES_PASSWORD: assetdesk

    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgressql/data

volumes:
  pgdata:

